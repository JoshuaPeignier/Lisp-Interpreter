#include <stdexcept>
#include <string>
#include <cassert>
#include "object.hh"
#include "eval.hh"

int is_subr(Object f);

Object subr_effect(Object l, Environment env);

Object subr_car(Object l, Environment env);
Object subr_cdr(Object l, Environment env);
Object subr_cons(Object l, Environment env);
Object subr_progn(Object l, Environment env);
